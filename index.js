const express = require('express');
const app = express();
const port = process.env.port | 4000;

// create get funtion 

app.get('/', (req,res)=>{
  
    res.setEn

});


app.listen(port, () => {
console.log(`port start : ${port}`);
});

